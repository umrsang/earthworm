<template><div v-if="open" class="game-dialog-backdrop" @click.self="$emit('close')"><section class="game-dialog game-contents-dialog" role="dialog" aria-modal="true"><header><h2>{{ $t('game.courseContents') }}</h2><button class="game-icon-button" type="button" @click="$emit('close')">×</button></header><ol><li v-for="(statement, index) in statements" :key="statement.id"><button type="button" :class="{ active: index === current }" @click="$emit('select', index)"><span>{{ index + 1 }}</span><strong>{{ statement.chinese }}</strong><small>{{ statement.english }}</small></button></li></ol></section></div></template>
<script setup lang="ts">import type { CourseStatement } from "../../api/course-pack"; defineProps<{ open: boolean; statements: CourseStatement[]; current: number }>(); defineEmits<{ close: []; select: [index: number] }>();</script>
